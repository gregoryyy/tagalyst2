{
  "name": "tagalyst2",
  "version": "0.1.5",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w -p tsconfig.json"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.255",
    "typescript": "^5.3.3"
  }
}
