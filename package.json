{
  "name": "tagalyst2",
  "version": "0.5.6",
  "private": true,
  "scripts": {
    "clean": "rm -rf .tsbuild content/content.js content/markdown.js content/content dom",
    "build": "tsc -b",
    "watch": "tsc -b --watch --preserveWatchOutput",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:e2e": "jest --runInBand test/e2e/puppeteer-smoke.test.ts",
    "dist": "npm run clean && npm run build && node scripts/build.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.255",
    "@types/jest": "^29.5.12",
    "cross-zip": "^4.0.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "puppeteer-core": "^24.15.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
