# Test Directory

This folder houses test-related scaffolding:
- `mocks/`: manual mocks (e.g., `chrome.ts`) used by Jest.
- `setupJest.ts`: global Jest setup (installs mocks).
- Future specs can live in `test/` alongside these helpers. Jest roots include `test/` and `src/`.
